import React from "react";
import MainComponent from "./application-area/MainComponent";


// this page will only be displayed, if user is logged in
export default function () {
    return (
        <MainComponent />
    );
}
